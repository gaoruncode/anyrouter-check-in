interface:
  display_name: "Anyrouter Check In"
  short_description: "Repo-specific patterns and workflows for anyrouter-check-in"
  default_prompt: "Use the anyrouter-check-in repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true